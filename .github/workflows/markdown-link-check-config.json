{
    // Regex: https://regexr.com
    "ignorePatterns": [
        {
            // ignore relative links, example: '/contact'
            "pattern": "/\/[^\/][\w\d]*/g"
        }
    ],

    "retryOn429": false,

    // ignore 0 and 429
    "aliveStatusCodes":[200, 206, 0, 429]
}
