{
    // Regex: https://regexr.com
    "ignorePatterns": [
        {
            // ignore relative links, example: '/contact'
            "pattern": "/^\/{1}[\w\d]*/g"
        }
    ],

    "retryOn429": false,

    // ignore 429
    "aliveStatusCodes":[200, 206, 429]
}
